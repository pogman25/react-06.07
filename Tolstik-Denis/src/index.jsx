import React, { Component } from 'react';
import ReactDOM from "react-dom";
import MessageField from './MessageField';

const rootElement = document.getElementById("root");
 
ReactDOM.render(<MessageField />, rootElement);
